let
 var a : Integer;
 var b : Integer
in
 begin
  getint (var a);
  getint (var b);
  putint (a + b);
  puteol();
  putint (a * b);
 
  if a = b then
   begin
    puteol(); put('S'); put('a'); put('m'); put('e'); put('!')
   end
  else;
  end
